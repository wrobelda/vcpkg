diff -Nur a/CMakeLists.txt b/CMakeLists.txt
--- a/CMakeLists.txt	2021-03-01 15:24:09.735431275 +0100
+++ b/CMakeLists.txt	2021-03-01 17:21:13.249328479 +0100
@@ -2,9 +2,14 @@
 
 set(KF5_VERSION "5.75.0") # handled by release scripts
 set(KF5_DEP_VERSION "5.75.0") # handled by release scripts
+set(PHONON_VERSION "4.11.1")
 
 project(KIO VERSION ${KF5_VERSION})
 
+if(VCPKG_TARGET_IS_LINUX)
+    message("${PORT} currently requires the following system packages: libudev\n\nThese can be installed on Ubuntu systems via apt-get install libudev-dev")
+endif()
+
 include(FeatureSummary)
 find_package(ECM 5.75.0 NO_MODULE)
 set_package_properties(ECM PROPERTIES TYPE REQUIRED DESCRIPTION "Extra CMake Modules." URL "https://commits.kde.org/extra-cmake-modules")
@@ -65,6 +70,8 @@
 find_package(KF5Solid ${KF5_DEP_VERSION} REQUIRED) # for kio_trash
 find_package(KF5GuiAddons ${KF5_DEP_VERSION} REQUIRED)
 find_package(KF5XmlGui ${KF5_DEP_VERSION} REQUIRED)
+find_package(KF5Attica ${KF5_DEP_VERSION} REQUIRED)
+find_package(Phonon4Qt5 ${PHONON_VERSION} REQUIRED)
 
 if (NOT KIOCORE_ONLY)
 find_package(KF5Bookmarks ${KF5_DEP_VERSION} REQUIRED)
@@ -87,6 +94,10 @@
     find_package(KF5Auth ${KF5_DEP_VERSION} REQUIRED)
 endif()
 
+if (CMAKE_SYSTEM_NAME MATCHES Linux)
+    find_package(UDev REQUIRED)
+endif()
+
 # tell what is missing without doctools
 set_package_properties(KF5DocTools PROPERTIES DESCRIPTION "Provides tools to generate documentation in various format from DocBook files"
                        TYPE OPTIONAL
@@ -94,7 +105,7 @@
                       )
 
 set(REQUIRED_QT_VERSION 5.12.0)
-find_package(Qt5 ${REQUIRED_QT_VERSION} CONFIG REQUIRED Widgets DBus Network Concurrent Xml Test)
+find_package(Qt5 ${REQUIRED_QT_VERSION} CONFIG REQUIRED Widgets DBus Network Concurrent Xml PrintSupport Test )
 
 find_package(GSSAPI)
 set_package_properties(GSSAPI PROPERTIES DESCRIPTION "Allows KIO to make use of certain HTTP authentication services"
